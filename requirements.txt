# Core — everything runs on these. No SHAP: explanations use exact logistic weights,
# permutation importance (global), and an occlusion method (local) — all built in here.
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3   # HistGradientBoostingClassifier handles NaN natively; no LightGBM needed
matplotlib>=3.7
joblib>=1.3
